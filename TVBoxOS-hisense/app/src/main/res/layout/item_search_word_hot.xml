<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/vs_66"
    android:layout_gravity="center_horizontal"
    android:layout_marginBottom="@dimen/vs_12"
    android:background="@drawable/bg_search_word_button"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?android:attr/selectableItemBackground"
    android:paddingStart="@dimen/vs_12"
    android:paddingEnd="@dimen/vs_12"
    android:visibility="visible">

    <TextView
        android:id="@+id/tvSearchWord"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:duplicateParentState="true"
        android:ellipsize="marquee"
        android:focusable="false"
        android:gravity="center"
        android:includeFontPadding="false"
        android:marqueeRepeatLimit="marquee_forever"
        android:singleLine="true"
        android:textColor="@color/search_word_text"
        android:textSize="@dimen/ts_20"
        android:visibility="visible" />
</FrameLayout>
